# SPEC 10/10 — Roteiro Executivo

**Score atual: ~7.4/10 | Alvo: 10/10 | Previsao: 5 batches**

---

## Batch 1 — Seguranca + Hooks (P0/P1)

| ID | Task | Esforco |
|----|------|---------|
| T1.6b | `email_confirm: true` no registro + fluxo de confirmacao | 1h |
| T2.1 | Hooks `use-products`, `use-inventory`, `use-movements` refatorados p/ use cases | 4h |
| T2.5 | Eliminar `any`, `as unknown as T`, casts inseguros | 2h |

## Batch 2 — Solid + Clean Code (P1/P2)

| ID | Task | Esforco |
|----|------|---------|
| T2.6 | Repository interfaces (`IProductRepository`, `IInventoryRepository`, etc.) | 3h |
| T2.2 | Strategy Pattern para tipos de movimentacao | 3h |
| T2.3 | Extrair Asaas God Object → 5 services modulares | 4h |

## Batch 3 — Seguranca + Performance (P1/P2)

| ID | Task | Esforco |
|----|------|---------|
| T3.4 | Cache `checkPlanAccess()` + Plan Configs (in-memory TTL) | 1h |
| T3.5 | Rate limiting por API Key na V1 API | 2h |
| T4.1 | Logger estruturado com `trace_id`, `user_id`, `tenant_id` | 2h |
| T4.2 | Global Error Handler + Sentry integration | 2h |

## Batch 4 — UX + Refinamento (P2/P3)

| ID | Task | Esforco |
|----|------|---------|
| T3.6 | Skeleton loading + Empty states (5 paginas CRUD) | 3h |
| T2.4 | Quebrar Settings page (260+ linhas) em 3 componentes | 2h |
| T4.3 | Logs estruturados de webhook Asaas | 1h |

## Batch 5 — Final (P3)

| ID | Task | Esforco |
|----|------|---------|
| T3.7 | Indices: `rate_limits.window_start`, `products.expiration_date`, `products.batch` | 1h |
| T4.4 | Metricas de negocio (signups, products, movements, api_requests) | 1h |
| T1.2 | Documentar plano PCI (tokenizeCreditCard requer redesign checkout Asaas) | 1h |

## Regras de Execucao

1. Cada batch termina com `npm run build && npm test` — zero erros
2. Tudo commitado + push a cada batch
3. Funcoes novas: max 20 linhas, sem `any`, sem casts forcados
4. Logger estruturado (console.log JSON) implementado no Batch 3, usado em diante
